@{
    ViewData["Title"] = "Shop";
}

<!-- We can pass one model that has lots of stuff in it; we had to use individual model views, though! -->
@model ProductListViewModel

<div class="page-buttons">
    <div page-model="@Model.PaginationInfo" page-action="Shop" page-class-enabled="true"
         page-class-normal="btn-outline-dark" page-class-selected="btn-primary" page-class="btn"
         page-url-legoType="@Model.CurrentLegoType" page-url-pageSize="@Model.CurrentPageSize" class="btn-group pull-right m-1">
    </div>
</div>
<div class="">
    <div class="row width">
        <!-- Filter section -->
        <div class="col-md-3 mb-4">
            <div class="row">
                <div class="col-md-12">
                    <div class="col-md-12 mt-3">
                        <h5>Show:</h5>
                        <a asp-controller="Home" asp-action="Shop" asp-route-pageSize="5">5</a>
                        <a asp-controller="Home" asp-action="Shop" asp-route-pageSize="10">10</a>
                        <a asp-controller="Home" asp-action="Shop" asp-route-pageSize="20">20</a>
                    </div>
                    <br />
                    <!-- Sidebar -->
                    <div class="sidebar" id="mySidebar">
                        <div class="col-md-12">
                            <h5>Filter by Category:</h5>
                            <!-- Include the contents of vc:lego-types here -->
                            <vc:lego-types />
                        </div>
                        <br />
                        <div class="col-md-12">
                            <h5>Filter by Color:</h5>
                            <vc:lego-colors />
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Products section -->
        <div class="col-md-9 contains_sets">
            <div class="row row-cols-1 row-cols-md-3">
                <!-- Updated class to handle varying column counts -->
                @foreach (Product p in Model.Products)
                {
                    <div class="col item">
                        <div class="prod">
                            <a href="@Url.Action("ProductDetails", "Home", new { id = p.ProductId })">
                                <img src="@p.ImgLink" class="product-image" alt="Product Image">
                            </a>
                        </div>
                            <a href="@Url.Action("ProductDetails", "Home", new { id = p.ProductId })">
                                <h3>@p.Name</h3>
                            </a>
                            <a href="@Url.Action("ProductDetails", "Home", new { id = p.ProductId })">
                                <h4>@p.Category</h4>
                            </a>
                            <h4>$@p.Price</h4>
                        </div>
                    </div>
                }
        </div>
    </div>
</div>





